// Minimal JS — currently adds a small console message and progressive enhancement hooks.
document.addEventListener('DOMContentLoaded', function () {
  console.info('Campus Boilerplate Hub — basic scripts loaded');
  // Placeholder for future enhancements (theme toggle, template preview, etc.)
});
